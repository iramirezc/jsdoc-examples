/** @module tags/@tutorial */

/**
 * @summary `@tutorial` block tag.
 * @description Example of how to link my tutorials using the `@tutorial` block tag.
 * @tutorial starter
 */
function blockTutorial () { /* noop */ }

/**
 * @summary `{@tutorial}` inline tag.
 * @description Example of how to link my custom tutorials using the `@tutorial` inline tag.
 * You may also want to start with the {@tutorial starter} tutorial
 * or go ahead and start experimenting with {@tutorial command-line}.
 * @note [Learn more about this @note tag here]{@tutorial tags}.
 */
function inlineTutorial () { /* noop */ }
