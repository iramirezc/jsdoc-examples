/** @module tags/@typedef */

/**
 * @typedef Vector
 * @type {Object}
 * @property {number} x - Point X
 * @property {number} y - Point Y
 */

/**
 * @summary `@type {Vector}`
 * @description Example of a defined type `Vector` with properties `x` and `y` of type `number`.
 * @example
 * aVector = { x: 1, y: 0 }
 * aVector = { x: -1, y: 3 }
 * @type {Vector}
 */
let aVector = { x: 0, y: 0 }