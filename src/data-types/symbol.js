/** @module data-types/symbol */

/**
 * Example of a `let` variable of type `symbol`
 * @type {symbol}
 */
let aSymbol = Symbol.iterator

/**
 * Example of a `const` constant of type `symbol`
 * @type {symbol}
 */
const aConstSymbol = Symbol.iterator
